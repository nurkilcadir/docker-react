language: generic 
sudo: required
services:
  - docker
before_install:
  - docker build -t nur1kilcadir/docker-react -f Dockerfile.dev .
script:  
  - docker run -e CI=true nur1kilcadir/docker-react npm run test
  